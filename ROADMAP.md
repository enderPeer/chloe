# CHLOE — Roadmap

- [x] **P1 — Foundation**: GAME_SPEC.md, image catalog, full game v1 (point-and-click scenes, battles, party/switch, items, give-up, XP/levels/skills, Shards currency, name+PIN accounts), landing page, Cloudflare Worker code, free image-gen pipeline (Pollinations), git repo.
- [ ] **P2 — Go live**: GitHub repo + Pages (landing+game) ✔ live, Cloudflare Pages mirror (second URL) pending, fix anything found on live. ~~Worker + KV cloud saves~~ (dropped — roguelike pivot, no saves).
- [x] **P3 — Combat v2 + The Room**: phase-based fighting (neutral/aggressive/guarded/staggered/charged), four move categories (attack/defense/stance/status), per-phase loadouts capped at 5, learnsets, loadout editor; new one-room horror opening (generated dressing room, clickable items, the_hollow enemy, solo Chloe start, Ash joins after clear). Spec sections 10-11.
- [x] **P4 — Progression v3**: 100-level cap with new XP curve, 11 damage types (physical/magical/lightning/fire/occult/blood/poison/divine/virus/ghost/biological) + authored type chart, 4 resources (Life/Stamina/Magic/Faith), buildup-based status system (burn/shock/bleed/poisoned/curse/infection/haunt), 45-60-node skill trees per character (3 branches, keystones, respec), skill points, character sheet, save v3. Spec section 12.
- [x] **P5 — Room3D**: pivot to first-person 3D (Three.js r128 vendored, zero build kept): one 3D dressing room with generated textures, WASD + mouse-look + keyboard fallback, AABB collision, luminance-keyed ghost billboard enemy — click it to enter the v3 round battle. Story/2D scene flow unrouted (code kept). Spec section 13.
- [x] **P5.6 — Room3D v2 photoreal**: HDRI environment map + ACES/PBR pipeline with shadows, real CC0 furniture models from Poly Haven (Sketchfab needs OAuth-gated downloads — not accessible), GLTF placement with computed colliders and box fallbacks, Space jump, first-person hands with walk sway, clickable TV on/off. Spec section 14.
- [x] **P5.7 — Roguelike pivot**: accounts/PIN/saves removed entirely (account.js + save.js deleted, no localStorage, no cloud); title -> straight into a fresh run; permadeath — defeat shows a run summary (level / shards / fights won) and "Begin again" restarts at level 1; page reload = new run. Spec section 15.
- [x] **P5.8 — Arena battles**: battles moved into 3D — real church asset (draco glb) + "Hollow Black Knight" (dark-knight asset, procedurally animated); turn-based attack picks for the whole band, then the knight's telegraphed swing must be DODGED in the room (crouch under slashes, sidestep lanes); the v2 hands gained open/close on the mouse buttons plus grab-in-motion item pickups, and Ctrl/C crouch; Blender LTS via winget as the asset pipeline. Spec section 16.
- [x] **P5.9 — Combat v3 (real-time)**: the arena fight is no longer turn-based — move/sprint/crouch/evade live while abilities fire off keys 1-9 with cast times, cooldowns, charges and stamina/magic costs. Level 1 = one key, one ability (fists); the skill tree grants both new abilities and extra keybinds, bound in Menu → Moves. First-person punch animation converted from the supplied Maya rig (147-bone deform skeleton isolated from ~2070 control empties, single `Punch` clip, 0.9MB) and fitted to the camera from the SKELETON (Box3 lies about skinned meshes). Church materials rebuilt from their baked emission atlases (base colour on UV0) + brighter playable light rig + `assetVersion` cache-busting. Spec section 17.
- [x] **P5.10 — Knight animation + Fire Tornado**: the knight now hunts you — always faces the player, walks to closing range and dashes across the nave on a cooldown, with arms, legs, torso and sword animated from limb pivots built out of his 103 named armour pieces (he has no skeleton). Overhead sword stroke and wide sweep poses. Hotbar shows cost + cooldown seconds per key. New Fire Tornado ability: raise a hand, trace a sigil off the fingertips, drop a fire funnel on him. Spec section 18.
- [x] **P5.11 — Ladder, party, displays, destructible nave**: skill tree became a shared 1-100 unlock ladder (L1 fists, L2 Fire Tornado, L3 Ash joins at her own level 1); leader death now hands the fight to the next member instead of ending the run; the mirror shows your stats, the poster shows the knight's, and the TV plays a chaptered how-to programme; the arena grew to the church walls with loose benches you shove aside and smash into wood piles. Spec section 19.
- [x] **P5.12 — Knight v2 + open nave**: benches deleted and the fight area widened to the measured walkable floor; the knight got a movement brain (stalk/press/strafe/reposition/recover/stagger, arc-biased approach, telegraphed lunge, per-knight personalities), two new attacks (thrust combo, ground slam with a radial shockwave), feints, a stagger punish window, and a real animation library (strafe, backpedal, turn-in-place, hit flinch, taunt, and a collapse death instead of sinking through the floor). Spec section 22.
- [x] **P5.13 — Pockets + asteroid stun**: asteroid dropped to 14 mana (it cost more than the earlier, stronger Fire Tornado and emptied a level-3 pool in one cast) and now STUNS every knight in its splash by reusing the §22 stagger state; bandages and mana potions are combat-usable and bindable to keys 1-9, with 2 extra pocket slots so no ability is sacrificed, a shared consumable cooldown, and a use-lock that leaves you hittable while you heal. Spec section 23.
- [ ] **P6 — Content & depth**: shop (spend Shards), more rooms/enemies, boss polish, sound/music.
- [ ] **P7 — Polish**: animations, mobile UX, balance pass, achievements.
- [ ] **P8 — Feedback round**: whatever the playtest surfaces.
- [ ] **P9 — Release QA**: final sweep, README/landing final, version 1.0.

## Key decisions (don't re-litigate)
- Zero-build vanilla JS, classic scripts, `window.CHLOE.*` namespace, runs from `file://` and any static host.
- **Roguelike (spec §15): no accounts, no persistence.** One run per page load, permadeath. The cloud-save Worker/KV plan is dead (`worker/` kept only as reference).
- Hosting free: GitHub Pages (primary) + Cloudflare Pages (mirror).
- Art: 111 existing photos in `game/assets/chloe/` (catalog in `tools/catalog/`); new art via `tools/generate-image.ps1` (Pollinations.ai, free, keyless).
- Machine: Windows 11, gh CLI authenticated; Node LTS installed.
